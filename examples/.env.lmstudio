# DocTagger Configuration for LM Studio
#
# LM Studio provides an OpenAI-compatible API on http://localhost:1234
# Make sure to:
# 1. Start LM Studio
# 2. Load a model (e.g., GLM-4, Llama 3, Mistral)
# 3. Start the local server (Server tab)

# Folder paths
INBOX_FOLDER=/path/to/inbox
ARCHIVE_FOLDER=/path/to/archive

# LM Studio settings (OpenAI-compatible)
LLM_PROVIDER=openai
LLM_MODEL=your-loaded-model-name
LLM_OPENAI_BASE_URL=http://localhost:1234/v1
LLM_OPENAI_API_KEY=not-needed

# OCR settings
OCR__ENABLED=true
OCR__LANGUAGE=eng
OCR__SKIP_IF_EXISTS=true

# Tagging settings
TAGS__MAX_TAGS=10
TAGS__CUSTOM_CATEGORIES=Invoice,Receipt,Contract,Letter

# Archive structure
ARCHIVE_STRUCTURE={year}/{month}/{document_type}

# Server settings
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
