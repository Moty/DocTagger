# DocTagger Configuration Example

# Folder paths
INBOX_FOLDER=/path/to/inbox
ARCHIVE_FOLDER=/path/to/archive
TEMP_FOLDER=/tmp/doctagger

# =============================================================================
# LLM PROVIDER SETTINGS
# =============================================================================
# Provider: 'ollama' (default) or 'openai' (for LM Studio, vLLM, etc.)
LLM_PROVIDER=ollama
LLM_MODEL=llama2

# Ollama settings (when LLM_PROVIDER=ollama)
LLM_OLLAMA_URL=http://localhost:11434

# OpenAI-compatible settings (when LLM_PROVIDER=openai)
# Use these for LM Studio, vLLM, text-generation-webui, or any OpenAI-compatible API
# LLM_PROVIDER=openai
# LLM_MODEL=your-model-name
# LLM_OPENAI_BASE_URL=http://localhost:1234/v1
# LLM_OPENAI_API_KEY=not-needed

# =============================================================================
# OCR SETTINGS
# =============================================================================
OCR__ENABLED=true
OCR__LANGUAGE=eng
OCR__SKIP_IF_EXISTS=true
OCR__DESKEW=true
OCR__FORCE_OCR=false

# =============================================================================
# TAGGING SETTINGS
# =============================================================================
TAGS__MAX_TAGS=10
TAGS__MIN_CONFIDENCE=0.5
TAGS__CUSTOM_CATEGORIES=Invoice,Receipt,Contract,Letter,Tax Document

# =============================================================================
# MACOS FINDER TAGS (optional, requires pyobjc)
# =============================================================================
MACOS_TAGS__ENABLED=false
MACOS_TAGS__COLOR_MAPPING=true

# =============================================================================
# ARCHIVE SETTINGS
# =============================================================================
# Available variables: {year}, {month}, {day}, {document_type}
ARCHIVE_STRUCTURE={year}/{month}/{document_type}
SIDECAR_ENABLED=true

# =============================================================================
# SERVER SETTINGS
# =============================================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# =============================================================================
# CLOUD STORAGE (optional)
# =============================================================================
# AWS S3 (install with: pip install "doctagger[s3]")
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_DEFAULT_REGION=us-east-1

# Google Cloud Storage (install with: pip install "doctagger[gcs]")
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Azure Blob Storage (install with: pip install "doctagger[azure]")
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string

# =============================================================================
# LOGGING
# =============================================================================
LOG_LEVEL=INFO
